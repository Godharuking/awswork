version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name lastwork --s3-location s3://lastwork-tatezawa/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src